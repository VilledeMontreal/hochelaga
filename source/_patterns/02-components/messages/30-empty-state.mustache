<div class="empty-state d-flex flex-column align-items-center py-8">
  <div class="mb-4">
    {{> styles-icon-item}}
  </div>
  {{# messageStateTitle}}<h2 class="mb-2">{{ messageStateTitle}}</h2>{{/ messageStateTitle}}
  <p class="mb-4">{{ description}}</p>
  {{# messageActions}}
  <div class="buttons-wrapper buttons-wrapper-center">
  {{# buttons}}
  {{> components-button}}
  {{/ buttons}}
  </div>
  {{/ messageActions}}
</div>
