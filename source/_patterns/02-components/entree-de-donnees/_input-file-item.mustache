<div class="form-group {{ formGroupClass }}">
  <div class="file-drop-container {{formGroupFileClass}}">
    <span class="help-text">{{ formGroupFileDropHelpText }}</span>
    <div class="file-selector-button">
      <input type="file" id="{{ formGroupFileDropID}}" class="sr-only"{{# formGroupFileState}} {{formGroupFileState}}{{/ formGroupFileState}}>
      <label class="btn btn-secondary" for="{{ formGroupFileDropID}}">{{ formGroupFileDropLabel }}</label>
    </div>
  </div>
  {{# formGroupInputError}}
    <div class="invalid-feedback" id={{id}}>
      <span class="icon icon-exclamation icon-xs" aria-hidden="true"></span>
      <span class="sr-only">Erreur</span>
      {{ text }}
    </div>
  {{/ formGroupInputError}}
</div>
