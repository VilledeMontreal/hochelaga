<button type="{{ buttonType }}" class="btn {{ buttonClass }}" {{ buttonState }}>{{# buttonIcon }}<span class="vfi vfi-{{ icon-label }}" aria-hidden="true"></span>{{/ buttonIcon}}{{ buttonText }}</button>
