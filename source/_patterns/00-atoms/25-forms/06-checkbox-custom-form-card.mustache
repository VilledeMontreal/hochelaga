{{# formCheckboxes }}
  {{# headings }}
    <label>{{ headings }}</label>
  {{/ headings }}
  <div class="custom-control custom-checkbox custom-checkbox-card">
    <input type="checkbox" class="custom-control-input" id="{{ id }}" {{ state }}>
    <label class="custom-control-label" for="{{ id }}">
      {{{ label }}}
    </label>
  </div>
{{/ formCheckboxes }}